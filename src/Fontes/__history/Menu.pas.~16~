unit Menu;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, Menus, Cadastros, Data.DBXFirebird, Data.DB, Data.SqlExpr;

type
  TFrMenu = class(TForm)
    mmMenu: TMainMenu;
    miParametros: TMenuItem;
    miProdutor: TMenuItem;
    miProduto: TMenuItem;
    miNegociacao: TMenuItem;
    SQLConnection1: TSQLConnection;
    procedure miProdutorClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FrMenu: TFrMenu;

implementation

{$R *.dfm}

procedure TFrMenu.miProdutorClick(Sender: TObject);
var
  frmCadastros: TfrCadastros;
begin
  frmCadastros := TfrCadastros.Create(Application);
  frmCadastros.ShowModal;
end;

end.
